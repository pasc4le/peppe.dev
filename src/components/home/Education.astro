---
import { getCollection } from "astro:content";
import EducationRenderer from "./EducationRenderer.astro";

const education = (await getCollection("education")).toSorted(
  (a, b) => +b.data.startDate - +a.data.startDate,
);
---

{education.map((entry) => <EducationRenderer {entry} />)}
