---
import type { IconLike } from './types.ts';

interface Props {
  icon: IconLike;
  class?: string;
}

const { icon, class: className } = Astro.props;
---

<svg viewBox="0 0 24 24" class={className}>
  <path d={icon.path} fill="currentColor" />
</svg>
